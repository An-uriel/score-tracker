<!DOCTYPE html>
<html>
<head>
  <title>Score Tracker</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script&display=swap" rel="stylesheet">

</head>
<body>
  <!-- Navigation Bar -->
  <div class="nav-wrapper">
    <div class="logo">🕵C Game Score Tracker</div>
    <div class="nav-links">
      <% if (currentUser) { %>
        <a href="/submit-score">Submit Score</a>
        <a href="/my-profile">My Profile</a>
        <a href="/logout">Logout</a>
      <% } else { %>
        <a href="/register">Register</a>
        <a href="/login">Login</a>
      <% } %>
    </div>
  </div>

  <!-- Hero Section -->
  <div class="hero">
    <div>
      <h1> 🔐 Welcome to Room Treasure Unlock 🔐</h1>
    <div class="hero-container">
      <p>Track your scores. Compete with others. Be the best!</p>
    </div>
    </div>
  </div>

  <!-- Message Section -->
  <div class="container">
    <div class="message-box">
      <p><a href="/leaderboard" class="btn">🏆 View Leaderboard</a></p>

      <% if (currentUser) { %>
        <p class="welcome-message">Bonjour! <strong><%= currentUser.name %></strong></p>
      <% } else { %>
        <p class="login-warning">
          <strong>You are not logged in.</strong><br>
           Register or log in to start tracking your scores.
        </p>

      <% } %>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    &copy; 2025 C Game Score Tracker. All rights reserved.
  </footer>
</body>
</html>
